services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '15432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cleaning_services
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
